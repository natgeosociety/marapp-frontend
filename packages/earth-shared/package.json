{
  "name": "@marapp/earth-shared",
  "version": "1.0.0",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "transpile": "babel src -d dist --ignore '**/*.spec.js,**/*.stories.js'",
    "test": "jest --passWithNoTests --color",
    "test:watch": "jest --passWithNoTests --watch --color",
    "test-coverage": "jest --coverage --color",
    "build-icons": "webpack --mode production"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react"
    ],
    "plugins": [
      "transform-class-properties"
    ]
  },
  "devDependencies": {
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "focus-trap-react": "^8.3.2",
    "lodash": "^4.17.20",
    "webfonts-loader": "^5.2.2"
  }
}
